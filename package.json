{
  "name": "gym-management-system",
  "version": "1.0.0",
  "description": "Integrated gym management platform with AI recommendations",
  "main": "dist/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "build": "npm run clean && tsc",
    "start": "node dist/index.js",
    "lint": "eslint src --max-warnings 0",
    "format:check": "prettier --check src",
    "format": "prettier --write src",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.0.3",
    "pg": "^8.8.0",
    "redis": "^4.6.0"
  },
  "devDependencies": {
    "eslint": "^8.40.0",
    "prettier": "^2.8.8",
    "typescript": "^5.0.4"
  }
}